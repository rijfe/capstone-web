import { styled } from "styled-components";

function ListBox() {
  return <ListBoxContainer>This is List Page</ListBoxContainer>;
}

export default ListBox;

const ListBoxContainer = styled.div`
  width: 100%;
  height: 100%;
`;
